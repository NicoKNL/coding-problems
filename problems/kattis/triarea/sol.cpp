#include <bits/stdc++.h>

using namespace std;

int main()
{
    fixed(cout);
    cout.precision(20);

    double base, height;
    cin >> base >> height;

    cout << 0.5 * base * height << endl;

    return 0;
}