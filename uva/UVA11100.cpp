#include <bits/stdc++.h>

using namespace std;

int main() {
    int n;
    while (cin >> n) {
        if (n == 0) break;
        vector<int> bags(n);
        for (int i = 0; i < n; i++) {
            cin >> bags[i];
        }
        sort(bags.begin(), bags.end());

        for (int i = 0; i < n; i++) {

        }
    }
    // TODO: finish
    return 0;
}