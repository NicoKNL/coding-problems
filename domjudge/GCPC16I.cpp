#include <bits/stdc++.h>
//todo: finish
using namespace std;

int main() {
    int n;
    cin >> n;
    long long sum = 0;
    sum = (long long) pow(4, n);
    sum *= n;
    cout << sum << endl;
    return 0;
}