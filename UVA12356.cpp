#include <iostream>
#include <vector>

using namespace std;

int main() {
    int s, b;
    while (cin >> s >> b) {
        if (s == 0 && b == 0) break;

        vector<int> soldiers;
        for (int i = 0; i < s; i++) {
            soldiers.push_back(1);
        }

        int left, right;
        while (b--) {
            cin >> left >> right;
            
        }
    }
}