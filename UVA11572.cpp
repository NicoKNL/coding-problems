#include <iostream>
#include <map>

using namespace std;

int main() {
    int n, flakes, f, length;
    cin >> n;
    while (n--) {
        cin >> flakes;
        length = 0;
        for (int i = 0; i < flakes; i++) {
            cin >> f;
        }
    }
    return 0;
}