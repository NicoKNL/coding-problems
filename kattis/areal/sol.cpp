#include <bits/stdc++.h>

using namespace std;

int main()
{
    fixed(cout);
    cout.precision(10);

    double n;
    cin >> n;

    double result = 4 * sqrt(n);
    cout << result << endl;
    return 0;
}