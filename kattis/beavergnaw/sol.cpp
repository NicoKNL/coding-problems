#include <bits/stdc++.h>

using namespace std;

int main()
{
    double D, V;

    while (true)
    {
        cin >> D >> V;
        if (D == 0 && V == 0)
            break;

        double v_total = M_PI * (D / 2) * (D / 2) * D;
        double d
    }

    return 0;
}